Switch to Strategy.

Roles rol = Roles.Usuario;

switch (rol)
{
    case Roles.Usuario:
        // do something
        break;
    case Roles.Administrador:
        // do something
        break;
    case Roles.Invitado:
        // do something
        break;
    default:
        break;
}